rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for local development (no auth)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
